name: Trường PTDT Nội Trú TJ+HCS - THPT Vân Hồ

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Cài đặt Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Cài đặt Java JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Cài đặt Android SDK
      uses: android-actions/setup-android@v3

    - name: Cài đặt Apache Cordova
      run: npm install -g cordova

    - name: Cấu hình Cordova Android
      run: |
        cordova platform add android
        
    # SỬA CHỖ NÀY: Đổi từ --release sang --debug để ép nổ máy ra file APK không cần chứng chỉ
    - name: Biên dịch ra file APK Debug
      run: cordova build android --debug

    # SỬA CHỖ NÀY: Chỉ đích danh vị trí file debug mặc định của Cordova
    - name: Xuất bản file APK về máy
      uses: actions/upload-artifact@v4
      with:
        name: VanHoApp-Debug-APK
        path: platforms/android/app/build/outputs/apk/debug/app-debug.apk
